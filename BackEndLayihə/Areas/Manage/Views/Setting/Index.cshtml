@model Setting
@{
    ViewData["Title"] = "Index";
}


<div class="container">
    <div class="row">
        <div class="col-12">
            <table class="table table-dark">
                <thead>
                    <tr>
                        <th>
                            #
                        </th>
                        <th>
                            Logo
                        </th>
                        <th>
                            Logo Title
                        </th>
                        <th>
                            Settings
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>
                            @Model.Id
                        </td>
                        <td>
                            <img src="@Model.Logo" alt="logo_image" style="width: 200px; height: 70px;" />
                        </td>
                        @if (Model.LogoTitle == null)
                        {
                            <td></td>
                        }
                        else if (Model.LogoTitle.Length >= 50)
                        {
                            <td>@Model.LogoTitle.Substring(0, 50)....</td>
                        }
                        else
                        {
                            <td>@Model.LogoTitle</td>
                        }
                        <td>
                            <a asp-controller="setting" asp-action="edit" asp-route-id="@Model.Id" class="btn btn-warning btn-icon-text">
                                <i class="mdi mdi-file-check btn-icon-append"></i>
                                Edit
                            </a>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>